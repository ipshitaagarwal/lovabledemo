{
  "name": "notion-crm-enrichment-worker",
  "version": "1.0.0",
  "description": "CRM enrichment Worker for Notion powered by Parallel Task API",
  "main": "server/index.js",
  "scripts": {
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "dev": "node server/index.js",
    "worker": "node server/worker.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "express": "^4.21.0"
  }
}
